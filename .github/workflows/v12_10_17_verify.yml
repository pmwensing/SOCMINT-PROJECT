name: SOCMINT v12.10.17 Verify Superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    name: Superseded by v12.10.18 UI verification
    runs-on: ubuntu-latest
    steps:
      - name: Explain supersession
        run: |
          echo "SOCMINT v12.10.17 verifier is superseded on v12.10.18 release UI branches."
          echo "Use .github/workflows/v12_10_18_verify.yml instead."
