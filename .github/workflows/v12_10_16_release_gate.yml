name: SOCMINT v12.10.16 Release Gate Superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    name: Superseded by v12.10.17 release status verification
    runs-on: ubuntu-latest
    steps:
      - name: Explain supersession
        run: |
          echo "SOCMINT v12.10.16 release gate is superseded on v12.10.17 post-merge verification branches."
          echo "Use .github/workflows/v12_10_17_post_merge_verify.yml instead."
