name: SOCMINT v12.10.18 Verify Superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    name: Superseded by v12.10.19 dashboard decision gate
    runs-on: ubuntu-latest
    steps:
      - name: Explain supersession
        run: |
          echo "SOCMINT v12.10.18 verifier is superseded on v12.10.19 decision-engine branches."
          echo "Use .github/workflows/v12_10_19_verify.yml instead."
