# v20.0 Case Findings Workspace

Adds the authenticated case findings workspace at `/case-findings/<case_id>` with proposed, approved, revision-required, rejected, and promoted finding states.

The workspace reuses immutable `audit_logs` events and introduces no migration.
