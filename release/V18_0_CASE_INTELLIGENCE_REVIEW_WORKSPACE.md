# v18.0 Case Intelligence Review Workspace

Adds the authenticated case-level analyst workspace and API composition layer at `/case-intelligence-review/<case_id>` and `/api/v1/case-intelligence-review/<case_id>`.

The workspace consumes existing case intelligence payloads and does not introduce a parallel persistence model or schema migration.
