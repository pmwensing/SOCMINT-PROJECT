# v18.6 Case Review Session History

Adds a capped, case-filtered Flask session timeline of analyst decisions with operator, note, decision, and timestamp. The history endpoint is `/api/v1/case-intelligence-review/<case_id>/history`.

No database persistence or migration is introduced.
