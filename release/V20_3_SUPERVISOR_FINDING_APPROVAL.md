# v20.3 Supervisor Finding Approval

Adds explicit supervisor approve, reject, and return-to-analyst decisions for proposed findings.

Each decision is an immutable audit event with actor, note, and timestamp. No migration is introduced.
