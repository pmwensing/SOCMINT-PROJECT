# SOCMINT v10.1.1 — Version Metadata Sync

## Summary

Synchronizes release metadata after the v10.1.0 action route readiness gate merge.

## Changes

- Updates `pyproject.toml` to `10.1.1`.
- Updates `src/socmint/__init__.py` to `10.1.1`.
- Adds focused version metadata validation tests.
- Adds this release note.

## Validation

Full CI must pass before merge.
